default: my_compare

my_compare:
	cmp -s "f1.txt" "f2.txt"; \
    RETVAL=$?; \
    if [ $RETVAL -eq 0 ]; then \
            echo "SAME"; \
    else \
            echo "NOT SAME"; \
    fi

print1:
	cat ./example1.txt

print2:
	cat ./example2.txt